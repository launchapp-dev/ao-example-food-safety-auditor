schedules:
  - id: daily-preop
    cron: "0 5 * * *"
    workflow_ref: daily-preop-check
    enabled: true

  - id: weekly-facility-audit
    cron: "0 9 * * 2"
    workflow_ref: facility-audit
    enabled: true

  - id: monthly-comprehensive-audit
    cron: "0 9 1 * *"
    workflow_ref: facility-audit
    enabled: true

  - id: capa-followup
    cron: "0 14 * * 3,5"
    workflow_ref: corrective-action-followup
    enabled: true
